#ifndef __GRAPH_TYPEDEFS_HPP
#define __GRAPH_TYPEDEFS_HPP

#include <vector>
#include <list>
#include <string>

using std::vector;
using std::list;
using std::string;

class Node;
typedef vector<Node *> NodeVector;

typedef list<string> IdList;

#endif
